import type { AcademyArticle } from './types'

const publishedAt = '2026-09-10T00:00:00.000Z'

export const academyBuildArticles: AcademyArticle[] = [
  {
    id: 'academy-build-auth',
    slug: 'does-your-mvp-need-login',
    title: 'MVP 第一版到底要不要做登录？先看它是不是核心任务的前提',
    summary: '登录很容易被当成“正式产品标配”，但它会带来账户、邮件、密码恢复、数据删除和安全责任。第一版要不要做，应该从核心任务是否需要身份连续性判断。',
    category: 'build',
    series: '从 0 到 1',
    created_at: publishedAt,
    updated_at: publishedAt,
    view_count: 0,
    author: { username: '小概率编辑部' },
    sources: [
      { label: 'Tally：产品与账户使用说明', url: 'https://tally.so/help/create-a-tally-account' },
      { label: 'Quick I Ching：公开版产品边界', url: 'https://github.com/pyxm1618/quickiching' },
    ],
    content: `新产品最常见的一组页面是：首页、注册、登录、忘记密码、个人中心。它们让产品看起来完整，却不一定让用户更快完成核心任务。

要不要做登录，不该从“别人都有”判断，而要先问：用户第一次获得价值，必须知道他是谁吗？

## 身份连续性有用，才值得付出账户成本

需要登录的典型场景很清楚：用户要跨设备保存长期数据；多人协作需要区分权限；购买记录、订阅、积分要绑定主体；敏感信息不能只靠一条公开链接访问；业务必须知道谁创建、修改或删除了某项内容。

如果这些条件一个都没有，登录可能只是额外摩擦。例如一个计算器、转换器、临时生成器，用户的核心任务是“输入 → 得到结果”。强制注册不会提高结果质量，只会把第一次成功推迟。

Quick I Ching 的公开 SEO V1 就采用了这种边界：免费起卦不依赖生产登录、数据库、支付和 AI 凭据，完成记录可以由用户主动保存到浏览器本地。未来商业版如果需要云端历史、付费次数和个性化，再启用账户体系。这个例子不是通用答案，但它说明身份可以晚于核心任务出现。

## 登录真正带来的工作远比两个输入框多

一旦有账户，就要决定注册方式、邮箱验证、忘记密码、会话过期、第三方 OAuth、账号合并、注销、数据删除、封禁、异常登录和隐私说明。

如果再和支付绑定，还要处理更麻烦的问题：账户删了以后，订单、退款和财务记录怎么办？这些记录是否可以一起删除，要根据业务和适用规则判断，不能简单把用户主记录清掉就结束。

所以登录不是 UI 功能，而是一套身份基础设施。

## 第一版其实有三种选择

第一种完全匿名。适合一次性任务，不要求跨设备连续性。

第二种“先用后登录”。用户先完成核心动作，在保存、同步、分享或付费时再创建账户。很多工具更适合这种路径，因为用户先知道产品值不值。

第三种一开始登录。适合数据天然私密、多人协作、企业权限或账户本身就是产品的一部分。

关键不是哪个更高级，而是登录出现的时机是否和用户价值一致。

## 一个简单的判断表

上线前逐项回答：不登录，用户能不能完整体验最核心价值？数据必须跨设备保存吗？有没有多人权限？有没有必须绑定身份的付费权益？匿名使用会不会造成明显滥用或安全风险？

如果前几项大多是否，最后一项也可控，第一版通常有理由不做强制登录。

MVP 的任务是验证核心价值。登录如果不是价值的一部分，也应该接受同一个问题：它现在不做，会不会让验证结果失真？不会，就可以晚一点。`
  },
  {
    id: 'academy-build-ai-spec',
    slug: 'write-verifiable-ai-coding-task',
    title: '让 AI 写代码之前，先把任务写成一份能验收的施工单',
    summary: '“帮我把支付做好”不是开发任务。AI Coding 更稳定的前提，是把现状、边界、业务规则、禁止事项和验收条件写清，让模型知道什么叫完成。',
    category: 'build',
    series: '从 0 到 1',
    featured: true,
    created_at: publishedAt,
    updated_at: publishedAt,
    view_count: 0,
    author: { username: '小概率编辑部' },
    sources: [
      { label: 'creat-web：公开 README 与发布门禁', url: 'https://github.com/pyxm1618/creat-web' },
      { label: 'Quick I Ching：公开产品边界与验证命令', url: 'https://github.com/pyxm1618/quickiching' },
    ],
    content: `AI 写代码速度很快，所以很多失败也发生得很快。

最典型的任务是：“把支付功能完善一下”“全面优化 SEO”“把登录修好”。人类开发者看到这种任务也需要追问，模型却很可能直接开始改。最后代码不少，真正的业务规则反而被它顺手改掉。

解决办法不是研究一句更神的 Prompt，而是把开发任务写成能验收的施工单。

## 第一部分先写现状，不要让模型自己猜

至少说明仓库、技术栈、生产环境、相关模块现在做到哪里。更重要的是告诉它哪些事实已经成立。

例如：“收费模式是次数包，不是订阅”“只有任务真正生成成功才扣次数”“失败必须释放冻结额度”。这些不是实现建议，而是业务规则。模型不能因为某个支付 SDK 更适合订阅，就顺便把产品改成订阅。

如果有已经上线的模块、不能移动的目录、历史方案但尚未接入的代码，也要明确写出来。复杂仓库里，“存在”不等于“正在使用”。

## 第二部分写边界：这次不做什么

这是 AI Coding 特别需要的一段。人类工程师通常会凭经验判断不要顺便重构整个系统，模型却容易把“发现问题”理解成“应该一起解决”。因此可以直接写：不重新设计产品；不改商业规则；不扩大到移动端；不做无关性能优化；不要顺手更换框架。

边界越明确，任务越容易收口。

## 第三部分把模糊目标改成可观察状态

“支付正常”无法验收。可以拆成：创建订单成功；重复请求不会创建两笔订单；支付成功事件重复投递不会重复发权益；生成失败不消费次数；退款以后权益变化有确定结果；生产回调签名被验证。

每一条最好都能通过测试、数据库状态、接口响应或真实页面验证。

creat-web 这类模板项目会保留 typecheck、unit、integration、contract、security、SEO、commerce、E2E 等不同门禁。一次构建成功只能证明代码能被打包，不能证明业务链路成立。

## 第四部分告诉 AI 证据优先级

遇到文档、代码、生产状态互相矛盾时，模型需要知道信谁。已经锁定的业务规则优先；当前真实代码和数据库状态其次；历史设计文档只能提供背景；注释和 README 如果与运行事实冲突，需要先核实而不是盲从。

外部 API 也一样。支付、认证、框架能力会更新，涉及版本和当前行为时，应先看对应官方文档，而不是依赖模型记忆。

## 第五部分写清楚停止条件

很多任务失败不是修不好，而是没有停止条件。例如：“只修下面已经确认的 5 项；全部测试通过后停止，不继续做全面审计。”这会显著降低无限找 bug、无限扩范围的风险。

一份可执行任务至少有六块：背景和现状、目标、不可改变的业务规则、本轮范围、禁止范围、验收清单。涉及生产时，再增加部署、回滚和真实验证要求。

AI Coding 的瓶颈正在从“代码写不出来”变成“到底应该写什么、什么叫正确”。把施工单写清楚，就是把最贵的判断留在人手里，把大量实现工作交给机器。`
  },
  {
    id: 'academy-build-verification',
    slug: 'build-passed-is-not-production-ready',
    title: 'Build 通过不等于能上线：小产品至少要过哪几层验收',
    summary: '构建成功只证明依赖和代码能产出部署包。登录、支付、数据库迁移、权限和真实浏览器流程仍可能全部有问题。验收应该一层层缩小“不确定”。',
    category: 'build',
    series: '从 0 到 1',
    created_at: publishedAt,
    updated_at: publishedAt,
    view_count: 0,
    author: { username: '小概率编辑部' },
    sources: [
      { label: 'OWASP Application Security Verification Standard', url: 'https://owasp.org/www-project-application-security-verification-standard/' },
      { label: 'creat-web：公开验证流水线', url: 'https://github.com/pyxm1618/creat-web' },
    ],
    content: `“已经能 build 了”是开发里最容易制造错觉的一句话。

构建通过通常说明：依赖能安装、语法和打包链路没有阻断性错误、框架能够产出部署产物。它不保证用户能登录，不保证付款以后权益真的到账，也不保证管理员看不到别人的数据。

产品上线前，需要把不同风险拆成不同验收层。

## 第一层：静态检查

类型检查、Lint、编译和依赖安装属于这一层。它们便宜、速度快，应该尽早跑。

静态检查很擅长发现类型不一致、无效导入、明显语法问题，却看不到业务语义。例如一个退款接口把金额乘错 100 倍，只要类型还是 number，TypeScript 不会替你报警。

## 第二层：确定性业务测试

把重要规则做成单元或集成测试。例如积分只能扣一次、匿名用户不能读私有记录、过期订单不能再发货、同一个 webhook 重放不会重复加权益。这些规则越接近钱和权限，越不应该只靠人“点过一次”。

测试的价值不只是上线前。三个月以后改另一处代码时，它能告诉你旧规则有没有被破坏。

## 第三层：真实依赖链路

Mock 能证明自己的代码按预想调用了一个接口，但不能证明第三方真的接受。

认证回调 URL、支付 webhook、邮件域名、数据库权限、对象存储 CORS、生产环境变量，很多问题只有接到真实或官方 sandbox 才暴露。支付至少要跑 provider 测试环境，OAuth 要走一次完整回调，数据库迁移要在空库和已有数据路径都检查。

## 第四层：浏览器中的完整任务

接口都正确，页面仍可能坏。按钮被遮住、移动端弹窗超出屏幕、登录以后重定向错、付款成功页面读到旧状态、表单校验和后端规则不一致，都属于真实用户路径的问题。

至少把核心链路从用户入口走到最终结果，按风险覆盖未登录、已登录、失败和重试分支。

## 第五层：安全和权限

OWASP ASVS 把 Web 应用安全拆成身份、会话、访问控制、输入、API、数据保护等系统性要求。小产品没必要机械追求大型金融系统的全部等级，但“规模小”不是跳过权限边界的理由。

最基本的问题包括：服务端是否重新验证身份；用户能否改 URL 读到别人的对象；敏感接口是否依赖浏览器传来的角色；密钥有没有进入客户端包；预览环境会不会被搜索引擎收录。

## 第六层：生产环境冒烟

部署成功以后再做一次最小验证：打开真实域名、确认 HTTPS 和重定向、检查首页与核心接口、跑一条不会破坏真实数据的用户路径、观察日志和错误监控。

验收不是测试越多越好。页面文案错一个字可以上线后改；重复扣款、越权读取和数据迁移错误必须在前面拦住。

把风险分层以后，“能上线”不再是一个模糊感觉，而是一串证据。Build 只是第一张。`
  },
  {
    id: 'academy-build-domain',
    slug: 'domain-dns-ssl-deployment-explained',
    title: '域名、DNS、SSL、部署是什么关系？上线时别把四件事混在一起',
    summary: '域名是名字，DNS 决定名字指向哪里，部署平台真正运行代码，TLS/SSL 负责 HTTPS 身份和加密。四层任何一层错了，用户都可能只看到“网站打不开”。',
    category: 'build',
    series: '从 0 到 1',
    created_at: publishedAt,
    updated_at: publishedAt,
    view_count: 0,
    author: { username: '小概率编辑部' },
    sources: [
      { label: 'Cloudflare Learning Center：What is DNS?', url: 'https://www.cloudflare.com/learning/dns/what-is-dns/' },
      { label: 'Vercel：Adding & Configuring a Custom Domain', url: 'https://vercel.com/docs/domains/working-with-domains/add-a-domain' },
      { label: 'Cloudflare：What is an SSL certificate?', url: 'https://www.cloudflare.com/learning/ssl/what-is-an-ssl-certificate/' },
    ],
    content: `第一次上线网站时，最容易把“域名接好了”理解成“网站已经部署好了”。其实用户在浏览器输入一个网址以后，中间至少经过四层：域名、DNS、部署服务和 HTTPS 证书。

把它们拆开，排错会简单很多。

## 域名：你买的是一个可管理的名字

例如 example.com。注册商负责登记你对这个名字的使用权和管理入口，但注册商并不会自动运行你的代码。

域名到期、注册信息或 Nameserver 配置属于这一层。

## DNS：告诉全世界这个名字应该去哪里

DNS 可以理解成互联网的名称解析系统。A 记录把域名指向 IPv4 地址，AAAA 指向 IPv6，CNAME 常用来把一个名字别名到另一个主机名；不同平台还会用 TXT 做域名验证。

你可以在一家注册商买域名，把 DNS 托管给 Cloudflare，再把记录指向 Vercel。这三个角色完全可以是三家公司。

所以“我在 Vercel 加了域名，但打不开”时，要先确认 DNS 是否真的指到了 Vercel 要求的位置，而不是重复部署代码。

## 部署：代码到底在哪里运行

Vercel、Cloudflare Pages、自建服务器等负责真正托管构建产物或运行应用。

以 Vercel 为例，项目部署以后会先有平台域名。绑定自定义域名只是给这份部署增加一个新的访问入口。Vercel 官方流程也是先把域名加入项目，再根据提示配置 DNS；如果 Nameserver 不由 Vercel 管理，就需要在外部 DNS 服务商处增加它要求的记录。

部署失败和 DNS 失败是两类问题。前者可能是构建、环境变量、运行时错误；后者通常是解析记录、传播或域名配置。

## TLS/SSL：为什么浏览器愿意显示 HTTPS

HTTPS 不只是“地址栏有把锁”。证书帮助客户端验证正在连接的域名身份，并参与建立加密连接。

现代托管平台通常会在域名解析正确后自动签发和续期证书；Cloudflare 作为代理时又多了一层浏览器到 Cloudflare、Cloudflare 到源站的连接配置。

这也是为什么错误的代理或 SSL 模式可能造成重定向循环、证书错误：两端都在根据自己理解强制 HTTPS。

## 排错时按顺序看

第一，域名有没有过期，Nameserver 当前是谁。

第二，用 DNS 查询确认 A、CNAME、TXT 是否是预期值，而不是只看控制台“已保存”。

第三，查看部署平台的项目状态和平台预览域名是否能打开。如果平台域名也坏，先修部署。

第四，DNS 正确以后看平台是否已确认域名并签发证书。

第五，再检查 www 与裸域、HTTP 与 HTTPS 的重定向是否只有一套明确规则。

记住这张关系图就够：注册商让你拥有域名；DNS 告诉别人去哪；部署平台在那里运行网站；TLS 让这个连接以 HTTPS 安全建立。网站打不开时先判断是哪一层，再动手。`
  },
  {
    id: 'academy-build-payment',
    slug: 'payment-integration-is-state-machine',
    title: '第一次接支付，最难的不是付款按钮，而是重复请求、回调和状态',
    summary: '支付会遇到超时、重试、异步回调和重复事件。真正可靠的系统必须做到同一个业务动作不会被执行两次，并且不能把“用户跳回成功页”当成到账事实。',
    category: 'build',
    series: '从 0 到 1',
    created_at: publishedAt,
    updated_at: publishedAt,
    view_count: 0,
    author: { username: '小概率编辑部' },
    sources: [
      { label: 'Stripe：Idempotent requests', url: 'https://docs.stripe.com/api/idempotent_requests' },
      { label: 'Stripe：Receive events in your webhook endpoint', url: 'https://docs.stripe.com/webhooks' },
    ],
    content: `支付 Demo 很容易做：点一个按钮，跳到收银台，付款完成，再回到成功页面。

真正上线以后，麻烦从网络不稳定那一刻开始。用户点了付款，浏览器超时，于是又点一次；服务器请求支付平台成功了，但响应在路上断掉，于是代码重试；支付已经完成，webhook 过几秒才到；平台发现 webhook 没及时返回，又重新投递。

如果系统把每一次请求都当成新动作，结果可能是重复订单、重复扣款、重复发积分。

## 第一个概念：幂等

Stripe 官方 API 明确支持 idempotency key，用来安全重试创建或更新请求。相同 key 的后续请求会返回第一次请求的结果，从而避免因为网络重试再次执行同一操作。

但支付系统不能把幂等完全外包给支付平台。自己的数据库也要有业务唯一性：一个 checkout session、一个支付事件、一个订单发货动作，应该有稳定 ID 和唯一约束或事务保护。

否则 webhook 收到两次，平台没重复扣款，你仍然可能给用户发两次权益。

## 第二个概念：异步事实

用户从支付平台跳回成功页，只说明浏览器走到了这个 URL，不代表系统应该立刻把订单改成 paid。

成功页参数可以被手工构造，浏览器也可能在支付最终状态还没同步时先回来。更可靠的做法是：支付平台的签名事件或服务端查询成为付款事实来源，本地订单根据经过验证的事实更新。页面只读取当前订单状态并展示。

Stripe 的 webhook 文档也把支付确认、争议、周期性付款等列为典型异步事件，并要求服务端验证签名。

## 第三个概念：回调会重复

Webhook handler 要假设同一事件可能重新送达。

先记录 provider event id 是否处理过，再在事务内做状态变更和权益发放。处理成功后快速返回 2xx；复杂工作可以进入持久化任务队列，而不是让 webhook 请求一直等模型生成、发邮件或做大计算。

如果任务失败，要有可重试状态，而不是“接口报 500，下次看运气”。

## 第四个概念：付款和交付是两个状态

卖一个 PDF，支付成功后可以立即生成下载权限；卖 AI 解读，付款成功以后还要调用模型并产出结果；卖次数包，则付款成功是增加余额，真正使用时再消费。

这几个动作不能混成一个 paid 状态。至少要区分订单支付、权益和具体交付任务。这样生成失败时，才能明确是退款、重试还是释放预留次数，而不是靠人工猜用户现在到底欠没欠一次服务。

## 上线前至少做五个破坏性测试

连续点两次购买；创建订单响应超时后重试；同一个成功 webhook 重放两次；付款成功但交付任务失败；用户不经过收银台直接打开成功页。

再根据业务加入退款、取消订阅、支付失败等分支。

支付系统可靠的标准不是“正常付款一次成功”，而是异常发生时钱和权益仍然对得上。按钮只是入口，真正的支付产品是一台状态机。`
  },
]
